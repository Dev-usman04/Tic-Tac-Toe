{
  "name": "react-socket-app",
  "version": "1.0.0",
  "private": true,
  "description": "Simple React + Socket.io app",
  "main": "index.js",
  "scripts": {
    "start": "concurrently -k \"node server.js\" \"npm --prefix ./socket-client start\"",
    "first": "echo Installing server packages... && npm install && cd socket-client && echo Installing client packages... && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rengwu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.0.4",
    "socket.io-client": "^2.0.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
